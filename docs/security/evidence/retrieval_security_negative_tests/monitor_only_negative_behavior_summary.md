# Monitor-only Negative Behavior Summary
Monitor-only evaluation flags unsafe candidates and emits audit/finding/metric evidence while preserving candidate list (no live blocking/filtering behavior).
