Step 19C non-leakage validation:
- Tests verify decisions do not carry raw document/chunk fields.
- Tests verify secret/token/api-key/credential patterns are rejected from metadata.
- Live retrieval/vector blocking/filtering remains disabled.
- Enforce and shadow-deny modes remain inactive.
