# Environment Template Summary

The staging environment template uses placeholders only and explicitly keeps
security-layer enforce, shadow-deny runtime, live-blocking, and live-filtering
flags false.
